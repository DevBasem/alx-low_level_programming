#include <stdio.h>
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
int c;
unsigned long fib1 = 0, fib2 = 1, sum;
for (c = 0; i < 50; i++)
{
sum = fib1 + fib2;
printf("%lu", sum);
fib1 = fib2
fib2 = sum;
if (c == 49)
printf("\n");
else
printf(", ");
}
return (0);
}
