#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @x: first number
 * @y: second number
 *
 * Return: result of the two number addition.
 */
int add(int x, int y)
{
int result;
result = x + y;
return (result);
}
